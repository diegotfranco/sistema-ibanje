import * as repository from 'repositories/produto.repository'

export const listarTodos = repository.listarTodos
export const buscarPorId = repository.buscarPorId
export const criar = repository.criar
export const atualizar = repository.atualizar
export const deletar = repository.deletar
